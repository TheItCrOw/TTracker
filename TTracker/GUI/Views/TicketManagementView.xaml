<Page x:Class="TTracker.GUI.Views.TicketManagementView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TTracker.GUI.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TicketManagementView">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--Ticket Management Topic Intro-->
        <Border Background="LightSteelBlue" BorderBrush="Black" BorderThickness="1" Grid.Row="0" Grid.Column="0">
            <TextBlock Text="Ticket Management" TextAlignment="Center" VerticalAlignment="Center" FontSize="15" FontWeight="Bold"/>
        </Border>

        <!--Buttons of the right big menu-->
        <Border BorderThickness="1" BorderBrush="Black" Grid.Row="0"  Grid.Column="1">
            <Grid Background="LightSteelBlue">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button ToolTip="All Tickets" Background="LightSteelBlue" BorderBrush="Black" Grid.Column="0" BorderThickness="0.5">
                    <Image Margin="5 5 5 5" Source="pack://application:,,,/Resources/tasks.png"/>
                </Button>
                <Button ToolTip="Task Tickets" Background="LightSteelBlue" BorderBrush="Black" Grid.Column="1" BorderThickness="0.5">
                    <Image Margin="5 5 5 5" Source="pack://application:,,,/Resources/task-ticket.png"/>
                </Button>
                <Button ToolTip="Date Tickets" Background="LightSteelBlue" BorderBrush="Black" Grid.Column="2" BorderThickness="0.5">
                    <Image Margin="5 5 5 5" Source="pack://application:,,,/Resources/date-ticket.png"/>
                </Button>
                <Button ToolTip="Projects" Background="LightSteelBlue" BorderBrush="Black" Grid.Column="3" BorderThickness="0.5">
                    <Image Margin="5 5 5 5" Source="pack://application:,,,/Resources/project.png"/>
                </Button>

            </Grid>
        </Border>

        <!--Items Control of TaskTickets-->
        <ScrollViewer Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Visible">
            <ItemsControl x:Name="taskTicketControl" Background="White" ItemsSource="{Binding TaskTickets}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="0.5" BorderBrush="Black">
                            <Grid Margin="10 10 10 10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50" />
                                    <ColumnDefinition Width="200" />
                                    <ColumnDefinition Width="700" />
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Button ToolTip="Open" Height="25" Width="25" Background="White" BorderBrush="LightGray">
                                    <Image Margin="5 5 5 5" Source="pack://application:,,,/Resources/down-arrow.png"/>
                                </Button>
                                <TextBlock Text="{Binding Name}" Grid.Column="1" FontSize="15" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Text}" Grid.Column="2" FontSize="15" Margin="0 0 20 0"/>
                                <TextBlock Text="{Binding ProjectName}" Grid.Column="3" FontSize="15"/>
                                <Grid  Grid.Column="5">
                                    <Border BorderThickness="1" BorderBrush="Black">
                                        <ProgressBar Minimum="0" Maximum="{Binding ExpectedTime}" Value="{Binding UsedTime}" />
                                    </Border>
                                    <TextBlock Text="{Binding Progress}" FontSize="15" TextAlignment="Center"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>



        <!--Nvigation Menu on the left-->
        <Border Grid.Column="0" Grid.Row="1" BorderThickness="0 1 1 0" BorderBrush="Black" >
            <Grid Background="LightSteelBlue">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                    <RowDefinition Height="50" />
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>
            </Grid>
        </Border>

    </Grid>
</Page>
